---
 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
   roles:
   - configure_cicd_inst
   - create_user
   - install_ansible
   - install_tomcat
   - install_jenkins

