---
 - name: Roles config
   hosts: ci_cd_server
   become: yes
   become_user: root
   roles:
   - create_user
   - install_docker

 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
   roles:
   - install_tomcat
   - configure_cicd_inst
   - install_ansible
   - install_jenkins
