---
 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
   roles:

   - create_user
   - configure_cicd_inst
   - install_ansible
   - install_tomcat
   - install_jenkins

