---
 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
 - include: docker.yml
 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
   roles:
   - create_user
   - install_tomcat
   - configure_cicd_inst
   - install_ansible
   - install_jenkins
