---
 - name: Roles config
   hosts: ci_cd_server
   become: yes #todo убрать и расставить правильно где надо
   roles:
   - install_ansible
   - install_tomcat
   - install_jenkins
   - configure_jenkins
   - configure_cicd_inst